ft.run(main, view=ft.AppView.WEB_BROWSER) Para abrir Web

Para criar um novo ambiente dar, python -m venv .venv e depois .venv\Scripts\activate

flet run --android NomedoArquivo pra rodar no celular